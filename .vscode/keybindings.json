// Place your key bindings in this file to override the defaults
[
    {
        "key": "f7",
        "command": "runCommands",
        "args": {
            "commands": [
                "workbench.action.tasks.build",
                "wokwi-vscode.start",
            ]
        }
    },
]